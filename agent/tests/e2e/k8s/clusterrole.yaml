apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: bistouri-agent
  labels:
    app.kubernetes.io/name: bistouri-agent
    app.kubernetes.io/component: agent
rules:
  - apiGroups: ["bistouri.dev"]
    resources: ["bistouriconfigs"]
    verbs: ["get", "list", "watch"]
